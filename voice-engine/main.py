# path: /voice-engine/main.py
# This is the main file that runs the voice engine
# plug: https://github.com/invalidse

import ai       # import ai.py      --> ai.py processes the voice and returns the response
import tts      # import tts.py     --> tts.py converts the text to speech
import sst      # import stt.py     --> stt.py converts the speech to text